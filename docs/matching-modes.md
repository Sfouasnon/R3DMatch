# Matching Modes

## Scene

Scene mode analyzes sampled frames before any creative display transform. The emitted stop offset is intended to be technical and scene-referred.

## View

View mode resolves the active monitoring context and optionally applies a `.cube` LUT override before analysis. This lets the estimator follow the viewed transform when desired.

