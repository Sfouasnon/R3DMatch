# Validation Plan

For each clip:

- compare original render metadata
- compare generated sidecar-driven render
- compare alternate match mode render
- review summary statistics and frame grabs

The prototype currently writes analysis manifests, sidecars, and REDLine command plans so validation can be automated once the local REDLine flag mapping is finalized.

